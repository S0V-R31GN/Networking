# Lab 01.3 – Connecting Devices Using Switch (Layer 2 – Setting Up a LAN)

## Switch vs Hub
A switch is network hardware that connects devices to establish a local area network (LAN).

**Switch pros (vs hub):**
- Has memory — stores a MAC address table
- Knows which port the destination device is on, and forwards data only to that port
- A hub has no memory/intelligence — it broadcasts incoming data to *all* ports regardless of destination

## MAC Address Table

| Device | MAC Address | Interface/Port |
|---|---|---|
| PC1 (sender) | 00D0.D36D.755A | 1 |
| PC2 (receiver) | 0060.3ECA.7A87 | 4 |

## Behavior
- Sender sends data to port 1.
- If this were a **hub**, it would broadcast the data to all other ports.
- A **switch** forwards the data only to the specified destination port (4).

## Setup
- Assign each PC an IP address.

## Test
1. Open Command Prompt on the sending PC (PC0).
2. Ping the destination PC (PC3).
3. Switch to Simulation mode and start the capture.
   - Source: PC1
   - Destination: PC4

## Images
<img width="1114" height="456" alt="image" src="https://github.com/user-attachments/assets/01c8b61b-6aae-46ea-a7bb-f6d7e9854414" />
<img width="596" height="63" alt="image" src="https://github.com/user-attachments/assets/4123aa4a-264d-4993-b71c-17c8f2bec3fc" />
